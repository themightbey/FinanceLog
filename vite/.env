VITE_APP_VERSION=v5.1.0
GENERATE_SOURCEMAP=false

## Base path. When hosted via the Cloudflare Worker + Assets, the app
## lives at the root, so keep this as "/". If you ever deploy under a
## sub-path, set it here.
VITE_APP_BASE_NAME=/
